{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -q '\\.md$' && npx markdownlint-cli2 \"$f\" 2>&1 || true; }",
            "statusMessage": "Linting markdown..."
          }
        ]
      }
    ]
  }
}
